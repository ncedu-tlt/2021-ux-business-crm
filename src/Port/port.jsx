import React from 'react';
import * as ReactDOM from 'react-dom';
import './/index.css';

const Portfolio = (
   <div>Тут будет портфолио</div>
)

const root = document.getElementById("root");
ReactDOM.render(Portfolio, root);